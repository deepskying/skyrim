# Skyrim iconfont

The shared Iconfont project is named **skyrim** (project id `5226886`).

Download its generated package from Iconfont into this directory before making a release. Keep `iconfont.ttf`, `iconfont.woff`, `iconfont.woff2`, and the generated CSS together so both PrismaUI modules can use the exact same glyph map.
